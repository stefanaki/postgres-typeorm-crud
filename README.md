# Postgres CRUD API

Experimenting with TypeScript, Express, TypeORM, Postgres, Docker

## Installation

1. Get Docker
2. In the codebase directory execute:
    ```
    $ docker compose up -d
    ```
3. Use the API at http://localhost:8080/api/
